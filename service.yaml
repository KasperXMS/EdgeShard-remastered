# EdgeShard service specification
# Generated by: edgeshard service init
# Model: Qwen/Qwen2.5-7B-Instruct
# Layers: 28, Hidden: 3584
#
# Next steps:
#   1. Review and adjust settings below
#   2. edgeshard cluster snapshot -o cluster.yaml
#   3. edgeshard plan service.yaml --cluster-yaml cluster.yaml
#   4. edgeshard service deploy service.yaml

name: qwen2.5-7b-instruct
model:
  name: Qwen/Qwen2.5-7B-Instruct
  revision: main
  dtype: float16
runtime:
  backend: torch
  max_batch_size: 1
  max_sequence_length: 4096
scheduling:
  policy: default
  hints:
    estimated_model_memory_gb: 14.0
    num_layers: 28
deployment:
  backend: docker
  shard_image: edgeshard/shard:latest
  resource_limits:
    memory: 16Gi
